# 项目进展与改动记录（总账本）

> 本文件是**所有项目**的统一改动记录。规则：**任何项目（灾害链推理引擎 v2 / 基础设施灾损识别 / 实验 / 数据）有改动，都必须在此追加记录**（日期 + 项目 + 改动 + 状态）。
> 更新：2026-08-19 建立

---

## 记录格式（以后每笔照此追加）

```
### YYYY-MM-DD｜项目名
- 改动内容：
- 状态：（完成/进行中/失败+原因）
- 备注：
```

---

## 历史关键节点（回填）

### 2026-08-10｜调研阶段
- 多灾种预测模型全球调研（GitHub/arXiv/期刊/中文文献），产出调研报告 2 份
- 专项调研"暴雨→内涝→地灾"灾害链推理模型

### 2026-08-11｜DisasterLex 部署
- 本地部署 DisasterLex（EMNLP2026 灾害知识图谱 Agent）并测试 12 用例
- 修复 3 个 Windows 兼容 bug（resource/fork/recursion），产出测试报告 2 份
- 实验 1：灾害链贝叶斯网络（pgmpy，8 节点）；实验 2：pfdf 敏感性分析

### 2026-08-12｜v2 开发
- Trae 开发灾害链推理引擎 v2：40 节点、配置化、弹性输入、Plotly 可视化
- 郑州 7·20 数据验证：653 网格、准确率 83.92%（含局限：ERA5 分辨率）
- 上传 GitHub：guo478883458/disaster-chain-engine

### 2026-08-17~18｜地区化+多区域耦合
- RegionEngine：模板/北京 16 区配置包、气象+水文双通道耦合推理
- 修复耦合 bug（证据状态映射、downstream 一致性）
- v2 项目迁移至 `H:\实习\灾害链推理引擎-v2`（原 C 盘删除）

### 2026-08-18｜图像预处理模块 + 新方向启动
- v2 新增 tools/image_preprocess.py + text_preprocess.py + preprocess_api.py（内涝/滑坡分割 + 灾情文本要素抽取，≤1 秒）
- 启动独立新方向：基础设施灾损识别（水位/道路桥梁/积水/救助点）
- 数据下载：FloodNet 2343 张、AIDER 526 张、CASlandslides 1947 组、CrisisNLP-C 4935 条、水位尺 3315 张、RDD2020 26336 张、UFO 215 张

### 2026-08-19｜基础设施灾损 POC + 微调
- POC 完成：结构 20/20、道路/水位双管线跑通
- 微调训练启动（道路 RDD2020 + 水位尺）
- 新增数据集下载中：FloodIMG / RescueNet / Sen1Floods11 / STURM（Trae 执行）

### 2026-08-19｜磁盘清理（C/D/H 三盘）

| 盘 | 清理前空闲 | 清理后空闲 | 清理内容 |
|---|---|---|---|
| C: | 1.9GB | 11GB | Temp 临时文件 8.3GB、pip cache 78MB、残留空壳目录（桌面\实习） |
| D: | 2.2GB | 12GB | conda clean 2GB + 删除 D:na\pkgs 包缓存 12GB（envs 环境保留） |
| H: | 20MB | 25GB | （上一轮已清 zip 原件 16GB + pip cache）本轮无操作 |

- 百度网盘文件夹未动
- D:na\envs（17GB 环境）保留；删除 pkgs 缓存不影响已建环境，装新包会重新下载

### 2026-08-19｜磁盘清理（第二轮，新数据核对）

**Trae 新数据下载核对：**

| 数据集 | 位置 | 状态 |
|---|---|---|
| FloodIMG | H:\dev\disaster-data\image_datasetsloodimg（9321 文件，12G） | ✅ 完整 |
| RescueNet | H:\dev\disaster-data\image_datasets
escuenet（15G，train 段） | ⏳ 仅 trainset（org 2941 / label 3595） |
| STURM-Flood | H:\dev\disaster-data\image_datasets\sturm_flood（6.3G，S1 21602 瓦片完整） | ⏳ S1 完整，S2 部分（5350） |
| Sen1Floods11 | 未落盘（脚本重试 5 版失败，googleapis 下载中断） | ❌ 未下载 |

**清理明细：**

| 项 | 大小 | 说明 |
|---|---|---|
| C 盘 .cache/kagglehub | 12G | RescueNet 下载缓存（H 盘已有解压数据） |
| H 盘回收站 | ~96G | 6.9 万文件（旧压缩包/游戏/已删下载） |
| D 盘回收站 | 2.9G | — |
| RLBJ_v1.0_shapefiles.rar | 60M | 已解压到 rlbj_23-7 |
| CSS-HGAT-SSP.zip | 584M | 数据已在 v2 有副本 |

**清理后空间：C 44G / D 15G / H 108G 空闲**


### 2026-08-20｜v2 引擎新模型训练 + 端到端验证（Trae）
- RescueNet 洪水分割训练完成：30 epoch，mAP50(mask)=0.834，产出 flood_rescuenet.pt
- FloodIMG 检测训练完成：产出 floodimg_detect.pt
- 端到端测试 test_e2e.py 通过：flood(分割)/flood_detect(检测)/landslide(滑坡) 三任务输出正常，可映射 BN 状态
- 8 节点回归 test_validate.py 通过：4 场景 × 2 输出节点，偏差均 < 0.01
- flood 任务耗时 6.02s（4000x3000 大图），待下采样优化

### 2026-08-23~25｜基础设施训练完成（道路/水位）
- 水位尺：100 epoch 训练完成（8-23），权重 water_level_digit_detector.pt
- 道路：RDD2020 微调 30 epoch 完成（8-25，imgsz=320），mAP50=0.267，权重 rdd2020_yolov8n.pt
- 训练过程教训：CPU 并行训练互相拖累 4~6 倍 → 改串行；道路 imgsz 640→320 提速 4 倍

### 2026-08-26｜水位识别修复（人工标注 + GPU 重训 + 公式重写）
- 人工标注：labelme 167 张/846 个数字框（yolo_labels_manual）
- GPU 重训：RTX 4060，150 epoch 仅 13 分钟，mAP50=0.966（旧弱监督模型 0.744）
- 关键认知：水位尺数字从上到下 9,8,7,6,5...，每 0.1 米一刻度，被水淹没部分不可见
- 公式重写：水位 = 1.0 + 最小可见数字 N × 0.1 米（旧 GaoKangYu 三位数公式错误）
- 实测验证：MAE 9.57cm（20 张独立抽样），水位范围 120~190cm 合理，self_test 33/33
- 遗留：~10% 图识别失败、插值小数位未生效、少数误读


### 2026-08-26｜综合演示完成（识别+推理闭环）
- 演示材料整理至 `H:\实习\综合演示\`：3 张输入图 + 推理结果 JSON + 讲解文档
- 修复：基础设施 flood 权重统一为 flood_rescuenet.pt（原误用旧 flood.pt）
- 演示效果实测：积水 1095m²(中度)、水位 160cm(误差0.7cm)、道路损毁 2 处
- BN 输出：内涝风险 中55%/高30%/低15%，地灾概率 高39.7%
- 已知小问题：BN 结果对证据节点自身查询报错（不影响内涝风险/地灾输出），待修

### 2026-08-26｜郑州全景可视化 Dashboard 开发
- **改动内容**：开发郑州全景可视化 Dashboard，实现地图展示 + 置信度 + 灵活输入 + 阶段总结 + 链式递进预测
- **具体完成项**：
  - 下载郑州 GeoJSON：从阿里 DataV 获取 410100_full.json（12 区县），保存到 `H:\dev\disaster-data\zhengzhou_720\zhengzhou_geojson.json`
  - 创建郑州区域配置：`configs/郑州/regions.yaml`（12 区县属性）、`model.yaml`（40 节点继承模板）、`coupling.yaml`（气象+水文双通道耦合）
  - 扩展 fuse_infer.py：新增 `_extract_confidence()` 函数，提取水位识别置信度、道路检测各框 confidence、洪水分割结果，返回 `置信度摘要` 字段
  - 重写 dashboard.py：郑州全景可视化首页（5 个 Tab）
    - **地图总览**：Plotly Choroplethmapbox 地图，各区按内涝风险最高态着色（绿/黄/红），hover 显示详情和置信度，数据表展示各区风险
    - **区详情**：区选择 + 侧边栏图片上传（水位尺/道路/洪水现场）+ 参数编辑 + 推理按钮，显示概率分布图和六阶段结果总结
    - **链式预测**：当前时段显示 + "下一时段"按钮将推理结果递推为证据（内涝高→土壤含水量高→地灾概率上升），支持重置
    - **参数管理**：全局编辑各区 BN 参数（按类别分组），一键重置所有区为先验，一键推理全部区
    - **使用帮助**：功能说明
  - 六阶段结果总结：河道水位、道路桥梁损毁、低洼地形积水、救助地点损毁、城市内涝、山体滑坡，每项标注证据来源
- **验收自测**：
  - BN 推理引擎加载正常，40 节点模型推理正确
  - 郑州 7·20 场景：内涝风险=高 68.3%，地灾概率=高 54.5%
  - dashboard.py 语法检查通过，Streamlit 启动无报错
  - GeoJSON 12 区县加载正常，Choroplethmapbox 可渲染
  - fuse_infer 置信度摘要扩展通过
- **状态**：完成（待启动后实机验证上传图片→识别→推理全链路）
- **备注**：原有北京模式保留为侧边栏切换选项（未完整移植，标记为"请在原版 dashboard 中操作"）

### 2026-08-26｜Dashboard 修复与优化（6 项）
- **改动内容**：针对首版 dashboard 的 6 个问题进行全面修复与优化
- **具体修复项**：
  - **问题一：移除模式选择器** — 侧边栏删除北京/其他模式切换，固定为郑州全景可视化；所有"北京"相关文案、radio 选择器移除；代码不再加载/引用北京配置
  - **问题二：首屏加载优化** — 模型权重懒加载（`st.cache_resource` + 惰性单例 `_get_lazy_models()`，首屏不加载）；GeoJSON 和 BN 引擎用 `@st.cache_resource(show_spinner=...)` 缓存；首屏仅渲染先验数据，不触发全量推理
  - **问题三：加载反馈** — 首次加载显示 `st.status("正在加载灾害链引擎…")` → 完成后变"✅ 引擎就绪"；一键推理全部区用 `st.progress` 进度条 + 文案"正在推理第 3/12 区：金水区…"；单区推理用 `st.spinner`；链式预测用 `st.spinner("正在推演时段 2…")`
  - **问题四：置信度独立呈现** — 地图色块叠加文字标注（各区中心显示"风险态+概率%"）；色块 `opacity` 与置信度挂钩（置信度越高越实，`max(0.3, min(1.0, top_prob * 1.2))`）；六阶段总结每项旁加置信度水平条 HTML（`render_confidence_bar_html` 函数，绿/黄/红三色）
  - **问题五：链式预测概率对比修复** — 对比表正确展示相邻时段概率差值 + 变化箭头（↑/↓/→）；"重置到时段 0"只重置链式预测的递推状态（`zz_chain_initial_evidence` 快照），不重置地图总览和区详情；新增递推规则展示（`CHAIN_RULES` 可扩展列表，页面显示当前递推链）
  - **问题六：灾害类型结论模块** — 新增 `get_disaster_conclusion()` 函数，基于推理结果生成直观结论（主要威胁判定、次生灾害、四级分级：安全/关注/预警/危险）；区详情页顶部结论卡片（带颜色边框 + 建议）；地图 hover 显示主要威胁和等级
- **验收**：
  - 语法检查通过（`py_compile` OK）
  - 侧边栏无模式选择器，仅郑州可视化快速操作按钮
  - 首屏加载有 status 反馈，耗时操作有 spinner/progress
  - 地图色块有文字标注 + 透明度通道
  - 六阶段总结有置信度条
  - 链式预测对比表有概率差值 + 箭头，重置仅影响链式状态
  - 区详情和地图 hover 显示灾害结论卡片
- **状态**：完成

### 2026-08-26｜郑州真实数据证据重构（替换模拟硬编码）
- **改动内容**：将 dashboard 首屏"先验预测"从代码写死的模拟暴雨场景替换为 Grid500 真实数据驱动
- **具体完成项**：
  - 创建 `scripts/build_zz_evidence.py`：读 Grid500 CSV（31021 网格），按地理分区规则分三组（山区/城区/平原），计算组中位数，按验证实验离散化映射表转为 BN 节点状态，输出 JSON
  - 生成 `configs/郑州/district_evidence.json`：12 区各 16 个节点映射（含气象/水文/地形/城市节点），组内一致、组间差异显著
  - 重写 `get_default_zz_evidence()`：改为读 JSON 文件（`@st.cache_resource` 缓存），保留原函数签名兼容
  - 删除 41 行硬编码证据字典及西部 3 区特殊处理代码
- **真实数据分组特征**：
  - 山区（6204 网格：巩义/登封/新密）：海拔高/坡度陡/硬化率低/排水快 → 地灾风险高
  - 城区（7157 网格：中原/二七/金水/管城/惠济/上街）：海拔低/硬化率高/排水慢 → 内涝风险高
  - 平原（7915 网格：中牟/新郑/荥阳）：过渡特征，硬化率中/排水快
- **验收**：
  - 12 区均有证据，每组 4/6/3 区，组内完全一致
  - 分组差异与郑州西高东低真实地理格局一致
  - 城区不透水高/排水弱 → 内涝风险高于山区，与真实郑州 7·20 一致
  - dashboard.py 语法检查通过
- **状态**：完成

### 2026-08-26｜演示参数快速填充脚本 + 参数清单文档
- **改动内容**：创建演示参数清单文档、一键填充脚本、dashboard 自动加载演示参数功能
- **具体完成项**：
  - 创建 `H:\实习\综合演示\演示参数清单.md`：完整列出 40 个 BN 节点（16 个有真实数据来源 + 24 个保持先验，含状态集和来源说明）、3 类图片参数（水位/道路/洪水 → 映射节点）、手动可调参数说明、快速演示流程
  - 创建 `scripts/prepare_demo_params.py`：CLI 工具，支持 `--district`/`--all`/`--no-images`/`--wl`/`--road`/`--flood`/`--list` 参数，输出 `configs/郑州/demo_params.json`
  - 修改 `dashboard.py`：新增 `_try_load_demo_params()` 函数，启动时自动加载 demo_params.json 预填 zz_evidence 和 zz_uploaded；侧边栏新增"演示控制"区域，显示"✅ 演示参数已加载"状态 + "🗑️ 清除演示参数"按钮
- **验收**：
  - `--all --no-images` 生成 12 区 JSON（各 16 证据节点）
  - `--district 金水区` 生成单区含 3 张演示图路径
  - `--list` 打印各区配置摘要
  - dashboard.py 语法检查通过
  - 侧边栏有清除按钮，点击后清空所有演示参数并 rerun
- **状态**：完成

### 2026-08-26｜Dashboard 推演按钮拆分（三种来源）
- **改动内容**：重构地图总览页和参数页的推演入口，从单一按钮改为来源选择 + 独立推演按钮
- **具体完成项**：
  - 地图总览页：新增 `st.radio("推演数据来源")` 选择器（演示参数 / 先验数据 / 当前手动参数），按钮文案随来源变化（如"🚀 使用演示参数推演全部 12 个区"）
  - 参数页（⚙️ 全局参数管理）：同步添加来源选择器 + 按钮，复用同一 `run_infer_all_districts()` 函数
  - 新增 `run_infer_all_districts(engine, source)` 函数 + 三个子函数：
    - `_run_infer_all_demo()`：加载 demo_params.json 证据 + 图片识别 + BN 推理
    - `_run_infer_all_prior()`：纯先验 BN 推理（不覆盖 zz_evidence）
    - `_run_infer_all_manual()`：用 zz_evidence 现有内容推理
  - 新增 `zz_infer_source` 追踪最近一次推演来源（demo/prior/manual）
  - 六阶段总结和地图数据表的"证据来源"列改为根据 `zz_infer_source` 动态标注（演示参数 / 手动参数 / 先验）
- **验收**：
  - 语法检查通过（py_compile OK）
  - 三个来源入口均集成，切换时按钮文案同步更新
  - 六阶段证据来源标注与实际来源一致
- **状态**：完成

### 2026-08-26｜地图尺寸修复 + 多场景演示样本
- **改动内容**：修复郑州地图显示过小问题；扩展演示参数脚本增加 4 组预设场景
- **具体完成项**：
  - **地图修复**（`render_zz_map`）：zoom 7.5 → 9.2（聚焦郑州 12 区），height 550 → 680，margin 留白减小，标注字号 10 → 12
  - **多场景演示**（`scripts/prepare_demo_params.py`）：新增 `--scenario` 参数，4 组场景：
    - `full`：12 区全部 16 证据 + 3 张图/区（完整数据）
    - `partial`：仅金水区/巩义市/中牟县 3 区有数据，其余 9 区先验（样本不足）
    - `single`：仅金水区有数据，其余 11 区先验（极端稀疏）
    - `params-only`：12 区全部 16 证据，无图片（纯参数）
  - 场景构建函数 `build_scenario_*` + `build_empty_district()`：无数据区写入空 evidence/images
  - 场景摘要打印 `print_scenario_summary()`：输出有数据/先验区数量、证据节点数、图片数
  - **文档更新**：`演示参数清单.md` 增加"五、演示场景说明"章节，详细描述 4 组场景的配置、演示意义和切换建议
- **验收**：
  - 语法检查通过（dashboard.py ✅ + prepare_demo_params.py ✅）
  - 地图 zoom 9.2 + height 680，12 区清晰可点
  - 四个场景命令均可运行，demo_params.json 内容符合定义
  - partial 场景 9 区无证据，single 场景 11 区无证据
- **状态**：完成

### 2026-08-26｜地图加载提示 + 链式页地图尺寸修复
- **改动内容**：地图渲染阶段添加 spinner 提示；render_zz_map 增加 height 参数实现两页地图尺寸分离
- **具体完成项**：
  - `render_zz_map` 增加 `height: int = 680` 参数，函数体改用 `height=height` 替代硬编码
  - 地图总览页：调用 `render_zz_map(..., height=680)` + `with st.spinner("⏳ 正在渲染郑州地图…")` 包裹
  - 链式预测页：调用 `render_zz_map(..., height=480)` + `with st.spinner("⏳ 正在更新地图…")` 包裹
  - 全链路反馈：推演按钮（进度条）→ spinner（地图渲染）→ 地图展示，无静默白屏期
- **验收**：
  - 语法检查通过（py_compile OK）
  - 地图总览页 height=680 大图清晰
  - 链式预测页 height=480 地图与页面控件整体适配
  - 渲染期间均有 spinner 提示
- **状态**：完成

### 2026-08-26｜项目打包发布（路径可移植化 + 发布包结构）
- **改动内容**：全项目硬编码路径改为可移植路径配置；创建发布包 A（演示运行包）和 B（训练数据包）的脚本和结构
- **具体完成项**：
  - **路径可移植化（三级回退）**：
    - 新建 `v2/path_config.py`（`灾害链推理引擎-v2` 根目录）和 `infra/path_config.py`（`基础设施灾损识别` 根目录）
    - 三级回退：环境变量 `DISASTER_DATA_DIR` → 原 H 盘 → 包内 `data/`
    - 修改 `v2` 文件（5 个）：`dashboard.py`（GEOJSON_PATH/DEMO_DIR → path_config 导入）、`tools/fuse_infer.py`（INFRA_ROOT → `INFRA_PROJECT_DIR`）、`tools/image_preprocess.py`（全部数据集/模型路径 → path_config）、`tools/text_preprocess.py`（CRISIS_DIR → path_config）
    - 修改 `infra` 文件（3 个）：`models/water_level/config.py`、`models/road_bridge/config.py`、`models/flood_detection/config.py`（全部改为从 `path_config` 导入常量）
    - 本机零影响：H 盘存在时仍走原路径，行为完全不变
  - **发布包 A 结构**：
    - 新建 `README.md`：快速开始 3 步 + 环境要求 + 常见问题
    - 更新 `requirements.txt`：完整依赖清单 + PyTorch CPU 版安装命令
    - 新建 `启动Dashboard.bat`：双击启动 Streamlit + 依赖检查 + 自动打开浏览器
    - 包结构：`v2_engine/` + `infra_recognition/` + `data/` + `演示材料/`
    - 权重清单：flood.pt、flood_rescuenet.pt、floodimg_detect.pt、landslide.pt、water_level_digit_detector.pt、rdd2020_yolov8n.pt
  - **发布包 B**：
    - 新建 `scripts/package_training_data.py`：打包训练数据集为 zip64 单文件
    - 保留：image_datasets/ 全部原始数据、infra_datasets/ 全部原始数据、models/ 全部最终权重
    - 排除：yolo_seg_labels(15G)、yolo_* 转换产物、models/*_train/、runs/ 目录
    - 带进度打印 + 压缩率统计 + 中断保护
- **验收**：
  - 语法检查通过（v2 11 个文件 ✅ + infra 8 个文件 ✅）
  - 本机 H 盘存在时行为不变（三级回退设计）
  - 新机器无 H 盘时自动 fallback 到包内 `data/`
- **状态**：完成

### 2026-08-26｜实际打包执行（两个 zip 生成 + 回归验证）
- **改动内容**：执行实际打包，生成演示运行包和训练数据包，验证内容完整性
- **具体完成项**：
  - **新建 `scripts/package_release.py`**：演示发布包打包脚本（压缩率 90.1%，耗时 2.3 秒）
  - **包 A：`灾害链演示_v1.zip`**（52.0MB）
    - 内容：6 个权重 / 3 张演示图 / GeoJSON / xlsx 真值 / 代码 / 演示文档
    - 31 个文件，全部权重存在
    - 抽查：flood.pt(25.9MB) / flood_rescuenet.pt(6.6MB) / GeoJSON(404KB) / xlsx(308KB) / 3 张演示图
  - **包 B：`训练数据_v1.zip`**（46.8GB）
    - 148,287 个文件，扫描 116 秒，打包 28.5 分钟
    - 压缩率 89.6%（52.2GB → 46.8GB）
    - 内容：`image_datasets/` / `infra_datasets/` / `models/` 三层目录完整
    - 抽查：flood_rescuenet.pt ✅ / yolo_labels_manual(167 张) ✅ / 各数据集样本 ✅
    - 修复：`_filelist` → `filelist` 属性名错误（Python 3.12+ 兼容）
  - **回归验证**：
    - `path_config` 导入验证通过（v2 + infra 双项目）
    - 本机 H 盘路径仍生效（三级回退设计验证）
    - numpy ABI 不兼容为预存环境问题，与路径改动无关
- **状态**：完成
---

## 数据资产清单（截至 2026-08-19）

| 数据 | 数量 | 位置 | 状态 |
|---|---|---|---|
| 郑州 7·20 网格 | 31021 行 | H:\dev\disaster-data\zhengzhou_720 | ✅ |
| 北京 23·7 滑坡 | 15383 处 | H:\dev\disaster-data\rlbj_23-7 | ✅ |
| FloodNet | 2343 张 | image_datasets\floodnet | ✅ |
| AIDER | 526 张 | image_datasets\aider | ✅ |
| CASlandslides | 1947 组 | image_datasets\caslandslides | ✅ |
| CrisisNLP-C | 4935 条 | image_datasets\crisisnlp_c | ✅ |
| UFO | 215 张 | image_datasets\ufo | ✅ |
| 水位尺 | 3315 张 | infra_datasets\water_level | ✅ |
| RDD2020 | 26336 张 | infra_datasets\rdd2020 | ✅ |
| FloodIMG | 9200 张 | image_datasets\floodimg（下载中） | ⏳ |

### 2026-08-31｜数据流模式图片识别记录修复：后台线程改为全局队列+主线程合并
- **改动内容**：
  - **根因修复**：`_bg_recognize` 后台线程无 `ScriptRunContext`，直接写 `st.session_state` 返回空对象导致 `KeyError`（日志 09:22/09:23 两次完整 traceback 指向 `st.session_state.has no key "zz_evidence"`），识别结果永远写不进去，界面恒显"暂无图片识别记录"
  - **修复方案**：新增模块级全局队列 `_BG_IMAGE_RESULTS` + `threading.Lock()`，`_bg_recognize` 只做识别+入队（不碰 `session_state`），Fragment A 主线程（有 `ScriptRunContext`）轮询队列出队后完成：
    - `map_to_bn_states` 转证据
    - `st.session_state.setdefault()` 防御性初始化（防止缺 key 异常）
    - `engine.infer()` 单区推理
    - 更新 `zz_evidence` / `zz_results` / `_stream_image_records`
    - 即使 `district` 不在列表中，也保留识别结果展示（跳过 BN 推理）
  - **空 station 静默跳过修复**：`ingest_server.py` 图片端点增加空 station 回退默认（"郑州-惠济区"），防止 `station = "-"` 导致图片静默消失
  - **环境修复（已执行，勿回退）**：`tifffile` 已装入 conda 环境 `D:\ana\envs\disasterlex\lib\site-packages`（阿里云镜像），`PYTHONNOUSERSITE=1` 下 easyocr 正常导入
- **验收**：语法检查通过（dashboard.py + ingest_server.py ✅）
- **状态**：完成
- **备注**：变更保护清单已确认——`PYTHONNOUSERSITE=1` / 双 fragment 结构 / `infra_path_config` / `flood_rescuenet.pt` / 无 `st.rerun(scope="fragment")` 均保留

### 2026-08-31｜项目打包发布（进度包）
- **改动内容**：
  - 创建 `项目进度包/运行说明.md`：环境安装三步、启动命令、演示路线、常见问题 5 条
  - 修复 `综合演示/run_demo.py` 硬编码路径：`INFRA`/`V2` 改为基于 `__file__` 相对定位，删除 `YOLO_CONFIG_DIR` 废弃环境变量
  - 修复 `综合演示/启动Dashboard.bat` 硬编码路径：`cd /d` 改为 `%~dp0..` 相对定位，删除 `D:\ana\envs\disasterlex\python.exe` 硬编码
  - 打包数据：`data/models/`（flood_rescuenet.pt / water_level_digit_detector.pt / rdd2020_yolov8n.pt）+ `data/zhengzhou_720/`（GeoJSON × 2 + era5_hourly_evidence.json）
  - 排除：`__pycache__` / `.git` / `*.pyc` / `*.log` / `era5_results_cache.json` / `yolov8n-seg.pt` / 训练数据集
  - 验证：`DISASTER_DATA_DIR` 指向 `data/` 时 v2 + infra 两项目的 6 个关键路径全部可访问
- **验收**：语法检查通过（dashboard.py + ingest_server.py + run_demo.py ✅）；`path_config` 三级回退机制经 `DISASTER_DATA_DIR` 验证正常
- **状态**：完成
| RescueNet | 4494 张 | image_datasets\rescuenet（下载中） | ⏳ |
| Sen1Floods11 | 4831 张 | image_datasets\sen1floods11（下载中） | ⏳ |
| STURM-Flood | 21602 瓦片 | image_datasets\sturm_flood（下载中） | ⏳ |
### 2026-08-19｜基础设施灾损识别 — 新数据接入（洪涝方向）

- **改动内容**：新增洪水/低洼积水检测模块，接入 FloodIMG / RescueNet / STURM-Flood 三套数据
- **具体完成项**：
  - 数据审计：tools/audit_new_datasets.py（只读统计，输出类别/数量/对应率）
  - 数据准备：tools/prepare_floodimg.py（labelme JSON→YOLO 检测，103 个标注，10 类）
  - 数据准备：tools/prepare_rescuenet.py（分割 PNG→YOLO seg，2941 张 14947 个多边形，9 类）
  - 模型模块：models/flood_detection/ 含 config.py + inference.py（FloodDetector 类）
  - 推理接口：tools/infer_api.py 新增 task="flood"（检测）和 task="flood_seg"（分割）
  - 训练入口：tools/train.py 新增 flood_detection 分支，三模型统一 --resume 断点续训
  - 自测：tools/self_test.py 新增洪水检测测试用例
  - 文档：更新训练计划.md + 项目进展总账本
- **关键发现**：
  - FloodIMG 无 "Flood" 标签，仅有 10 类通用场景标注（Person/Tree/House/...），仅 25 张有标注
  - RescueNet 2941 对匹配，654 张额外标签无对应原图；像素值 0-10 含 9 类非背景
  - STURM S1 完整 21602 对，S2 不完整 2675 对；Floodmaps 为多值掩膜（0/2/5）
- **状态**：完成（待训练）
- **训练命令**：`python tools/train.py --model flood_detection --epochs 100`
- **续训命令**：`python tools/train.py --model flood_detection --resume`

### 2026-08-19｜基础设施灾损识别 — 洪水识别修正（接入 v2 分割模型）
- **改动内容**：将 flood 任务从 FloodIMG 物体检测改为 v2 项目已训好的洪水分割模型
- **原因**：FloodIMG 标注无 "Flood" 类，检测结果不可用（实测洪水图只检出"交通标志"）
- **具体完成项**：
  - 重写 models/flood_detection/inference.py：FloodDetector 加载 H:\dev\disaster-data\models\flood.pt（YOLOv8n-seg，26MB）
    - 主方法 infer() 复刻 v2 ImageSegmenter.predict() 逻辑（约 40 行），不跨项目 import
    - 输出：积水面积_m2、淹没占比、灾情等级、mask_pixels、image_size、inference_time_ms
    - 面积换算：PIXEL_AREA_M2 = 0.0025（1px = 0.05m），灾情等级阈值沿用 v2（无<100/轻度<500/中度<2000/重度<5000/严重>=5000）
    - 原 FloodIMG 10 类检测降级为 detect_objects() 备用方法
    - 权重不存在时明确报错，不回退 COCO
  - 更新 models/flood_detection/config.py：权重路径指向 flood.pt，新增 PIXEL_AREA_M2 / DISASTER_LEVELS 常量
  - 更新 tools/infer_api.py：flood/flood_seg/flood_detection 均走分割推理，删除"待训练后替换"note
  - 更新 tools/self_test.py：[2.3] 测试洪水淹没面积检测，断言积水面积_m2/淹没占比/灾情等级
  - 更新 训练计划.md：洪水行状态改为"已接入 v2 分割模型"，FloodIMG 检测标记为"放弃"
- **验证结果**：
  - 自测 33/33 全过（含原 water_level/road 无回归）
  - 推理耗时：热加载后 ~85ms/张（CPU），首次加载 ~1.5s
  - 输出示例：Flood_1011.jpg -> 积水85.1m2, 占比0.0709, 等级=无
- **状态**：完成
- **备注**：权重文件 flood.pt 只读引用，不修改 v2 项目任何文件

### 2026-08-26｜识别+推理融合（v2 宿主）
- **改动内容**：
  - map_to_bn_states 扩展：水位→河道水位（阈值<120cm/120~150cm/≥150cm）、道路损毁→道路积水历史频率（0/1~3/≥4处）及管网排水能力（≥1处→"弱"）
  - 内涝深度 5 态→3 态 bug 修复（<100m²→无/100~2000m²→浅/≥2000m²→深）
  - 地质灾害→地质易发性 5 态→2 态修复（<50m²不填/50~200m²→低/≥200m²→高）
  - 新增 tools/fuse_infer.py 综合推理入口（跨项目调用基础设施灾损识别结果 → BN 证据组装 → 灾害链推理）
  - 端到端验证：fuse_infer.py 三图组合跑通；test_e2e.py + test_validate.py 无回归
- **状态**：完成
- **备注**：多证据 vs 单证据概率对比：三证据（洪水+水位+道路）组合时内涝风险"中"~0.55，单洪水证据时"高"~0.3（多证据信息更充分，不确定性降低，高等级概率下降合理）

### 2026-08-26｜训练数据分卷打包（18 卷，每卷 ≤2.9GB）
- **改动内容**：将单 zip 训练数据包改为分卷 zip，支持分批上传
- **具体完成项**：
  - 修改 `scripts/package_training_data.py`：新增分卷逻辑（first-fit 算法，按文件大小降序排列，累积达 2.9GB 切下一卷），支持 `--vol-size-gb` 参数
  - 命名规范：`训练数据_001.zip` ~ `训练数据_018.zip`（zip64，每卷独立可解压）
  - 每卷保留原相对路径，接收方合并解压到同一目录即还原完整结构
  - 生成 `分卷清单.txt`：每卷序号/文件数/压缩后大小/原始大小/耗时
  - 修复排除规则：增加 `*_train` 目录排除（rescuenet_train 等训练产物）
  - 输出目录：`H:\实习\发布包\`
- **打包结果**：
  - 总卷数：18 卷
  - 总文件数：148,206（排除 81 个 `*_train` 文件）
  - 总原始大小：52.1GB
  - 总压缩后大小：45.3GB
  - 总耗时：90.7 分钟（含分卷规划 10 分钟）
  - 每卷 ≤2.9GB，前 12 卷约 2.9GB，后 6 卷 1.7~2.4GB
- **演示包**：`灾害链演示_v1.zip`（52.0MB）确认存在，未变动
- **验收**：
  - 分卷清单 ✅（18 卷，元数据完整）
  - 抽查第 5 卷：609 文件，`image_datasets`/`infra_datasets` 目录正确 ✅
  - 抽查第 18 卷：100,676 文件，含 `models` 权重，**0 个 `*_train` 文件** ✅
  - 磁盘清理：删除旧分卷后 H 盘 77.1GB 空闲，满足打包需求
- **状态**：完成

### 2026-08-28｜郑州地图加载性能优化
- **改动内容**：郑州 dashboard 地图渲染从数秒降到 <1 秒，四项优化并行
- **具体完成项**：
  - **GeoJSON 预简化**：使用 shapely 的 simplify(tolerance=0.003, preserve_topology=True) 简化郑州 12 区边界，顶点数从 4,480 减至 880（↓80.3%），文件大小从 404KB 减至 25KB（↓93.8%），落盘至 `zhengzhou_geojson_simplified.json`，`load_geojson()` 优先加载简化版
  - **合并为单 trace**：`render_zz_map` 从 12 次 `add_trace`（每区独立 GeoJSON 过滤 + 独立 trace）改为 1 次 `go.Choroplethmap` 单 trace 渲染，各区独立透明度编码到 RGBA 颜色中（`_hex_to_rgba()`），colorscale 12 段映射保持"低绿/中黄/高红"语义
  - **迁移 MapLibre 后端**：`go.Choroplethmapbox` → `go.Choroplethmap`，`layout.mapbox` → `layout.map`，使用 MapLibre/WebGL2 后端，style/center/zoom 保持原效果
  - **按需更新缓存**：`session_state` 中维护 `_map_cache_version`，每次推理结果变化时递增；`render_zz_map` 按 `map_v{version}_p{period}` 缓存 fig，相同版本+时段直接复用（仅更新标题）
- **量化数据**：
  - GeoJSON：原始 404KB/4,480 顶点 → 简化 25KB/880 顶点（↓93.8%/↓80.3%）
  - 构建耗时：原 12 trace 数秒 → 单 trace 平均 30ms（最快 5ms，↓99%+）
  - 首屏地图出现时间：<1s（目标达成）
- **验收**：
  - 语法检查通过（py_compile OK）
  - 单 trace 构建功能验证通过（1 个 trace, Choroplethmap 类型, layout.map 配置）
  - 简化 GeoJSON 12 区要素完整，区域名正确
  - 视觉语义不变（颜色/标注/交互逻辑保留）
  - 链式预测页 height=480 通过 height 参数传递，不受影响
- **状态**：完成

### 2026-08-28｜趋势预测层：基线 AR + LSTM 对比 + dashboard 预测集成
- **改动内容**：实现 ERA5 实时监测趋势预测层，包含预测引擎、dashboard 增强、方案对比
- **具体完成项**：
  - **新增 `scripts/forecast_engine.py`**：趋势预测引擎
    - 基线方案：AR(1) 最小二乘拟合外推（降水强度）+ 规则兜底 + 线性外推（土壤含水量）+ 经验公式（河道水位≈基准 + k·Σ降水）
    - 训练方案：LSTM（2 层、输入 6 步→输出 3 步），前 120/后 24 训练/验证划分
    - NeuralProphet 尝试失败（progress_bar 参数版本不兼容）
    - 方案对比：LSTM 在 swvl1 上准确率从 0%→100%（提升显著），tp_mm 持平（70.83%）
    - 最终采用：LSTM（提升显著），覆盖降水/土壤含水量预测
    - 输出 144 条预测记录（T+1/T+2/T+3 小时证据+置信度）→ `era5_forecasts.json`
  - **dashboard.py 增强**：
    - 趋势图新增 +1h 预测虚线（城区内涝/山区地灾 P(高)对比）
    - 地图时段切换按钮：当前 / +1 小时 / +3 小时（用预测证据推理风险地图）
    - 预测结论条：降水强度/河道水位/土壤含水量 + 置信度
    - 预测关键事件标记（预测风险首超 50% 的橙色标注）
  - **验证抽查**：
    - T=40（暴雨增强段）：预测降水=中→高，水位=警戒→危险，与实际峰值吻合
    - T=90（雨停段）：预测仍高（滞后效应，模型已申明延迟）
  - **方案对比结论**：swvl1 提升显著（0%→100%），决定采用 LSTM 方案覆盖降水/土壤含水量预测
- **状态**：完成
- **备注**：训练产物（.pt 模型）存 `H:\dev\disaster-data\models\forecast\`（不进 git）；预测引擎验证抽查通过；dashboard 语法检查通过，Streamlit 启动无报错；git commit 460738d

### 2026-08-28｜降水预测滞后修复：分变量方案选择
- **改动内容**：修复趋势预测层降水预测滞后问题，分变量选择预测方案
- **修复原因**：LSTM 在降水序列上过拟合"高降水→持续高"模式，T=90（7/21 雨停）预测仍为"降水=高(80%)"，未反映雨停转折；且 LSTM 与基线 AR 在降水序列上准确率持平（均 70.83%），LSTM 无优势却有滞后缺陷
- **具体完成项**：
  - **降水强度（tp_mm）—— 规则/AR 混合方案**：`scripts/forecast_engine.py` 新增 `predict_precipitation_hybrid()` 函数，使用原始 tp_mm 值（≤1mm/h）进行雨停检测；基于 12h 累计降水（≥25mm）和当前强度（≥3mm/h）区分"持续暴雨"与"温和回升"；新增 sum12≥25mm + 当前"高"的暴雨持续规则
  - **前期土壤含水量（swvl1）—— 保留 LSTM**：不变，LSTM 从 0%→100% 提升显著且土壤含水量变化平滑、滞后影响小
  - **河道水位—— 新增回落检测**：`predict_water_level_hybrid()` 新增基于原始 tp_mm 值的回落条件（过去 6h 均≤1mm/h + 降水"低"→正常；降水"中" + 12h 累计<25mm→正常）
  - **降水时长**：基于修正后的降水预测序列计算
  - **数据支持**：`scripts/build_era5_timeseries.py` 新增 `raw` 字段存储原始 tp_mm 等数值，支持精确阈值判断
  - **验证脚本**：新增 `scripts/_check_era5_t90.py`、`scripts/_check_raw_tp.py` 辅助调试
- **验证结果**：
  - T=60（7/20 暴雨中，2021-07-20T12:00）：降水=高(65%)，水位=危险 ✅ 保持原预期
  - T=90（7/21 雨停，2021-07-21T18:00）：降水=中(60%)，水位=正常 ✅ 修复前为"高/危险"，修复后正确回落
  - T=120（7/22 恢复期，2021-07-23T00:00）：降水=低(70%)，水位=正常 ✅ 保持原预期
- **状态**：完成
- **备注**：git commit 含"降水预测滞后修复：分变量方案选择"

### 2026-08-28｜实时监测预计算磁盘缓存优化
- **改动内容**：实时监测页首次进入 170 秒全量预计算优化为磁盘缓存加速
- **具体完成项**：
  - `path_config.py` 新增 `ERA5_RESULTS_CACHE_PATH` 常量
  - `dashboard.py` 新增 `load_era5_results_cache()` / `save_era5_results_cache()` 函数
  - 缓存失效检测：基于 `demo_params.json` 和 `era5_hourly_evidence.json` 的 MD5 hash 双校验
  - 预计算逻辑改为：先检查磁盘缓存，有效则秒开加载（<2 秒），无效才全量预计算
  - 首次预计算 status 文案优化为"⏳ 首次预计算 144 小时推理（约 3 分钟），完成后将缓存加速"
  - 进度条改为百分比显示 `⏳ 预计算中 50/144 小时 (35%)…`
  - 完成后缓存写磁盘，下次进入/刷新即秒开
- **验证结果**：
  - 语法检查通过 ✅
  - 首次进入：约 3 分钟 status 完成，缓存写入磁盘
  - 再次进入/刷新：缓存命中，秒开（<2 秒），趋势图立即显示
  - 修改 demo_params 后：hash 不一致，自动重算
- **状态**：完成
- **备注**：git commit 119f2da，含"实时监测预计算磁盘缓存：缓存命中秒开，hash失效检测"

### 2026-08-31｜数据流模式"最近图片识别"无记录修复（tifffile 环境错位）
- **现象**：实时监测-数据流模式下，"📷 最近图片识别"始终显示"暂无图片识别记录"；road/flood 识别正常，water_level 报"请安装 easyocr"
- **根因**：
  - dashboard 后台线程 `_bg_recognize` 会全新 import easyocr；easyocr 依赖 tifffile
  - tifffile 之前只装在**用户级**目录 `C:\Users\郭宇博\AppData\Roaming\Python\Python310\site-packages`（pip 报"already satisfied"指的就是这里）
  - dashboard 启动使用 `PYTHONNOUSERSITE=1`（启动Dashboard.bat 设置），用户级目录被排除在 sys.path 外 → 后台线程 import easyocr 报 `No module named 'tifffile'` → handler 返回 error → 记录被 `if "error" not in result` 跳过
  - 主线程看似正常是因为 easyocr 已被早先代码缓存 import（tifffile 由其他包连带加载）
- **修复**：把 tifffile 2025.5.10 显式装入 conda 环境 `D:\ana\envs\disasterlex\lib\site-packages`（aliyun 镜像），与用户级版本同版本共存
- **验证结果**：
  - `PYTHONNOUSERSITE=1` 下后台线程 `import easyocr` → OK ✅
  - 后台线程跑真实图片全链路：water_level 水位 160cm/置信度 0.94（之前此处返回 error）、flood 积水 4445m²/重度 ✅
  - dashboard 已重启（health OK），ingest_server(8502)、era5 推流、图片推流进程保持运行
- **状态**：完成
- **备注**：若换机器/换环境重现，检查 easyocr 依赖是否被用户级包占位；变更保护：dashboard.py 的 `_bg_recognize` 与 `if "error" not in result` 逻辑勿改动

### 2026-08-31｜项目内容全面整理（U盘传输用）
- **改动内容**：清理重复文件 + 整理全部项目内容到 `U盘传输-20260831/` 总目录
- **清理明细**：
  - 删除 H:\实习 根目录：灾害链推理引擎-v2.zip（旧包）、项目进度包.zip 和 项目进度包/（精简包，已被全面整理取代）
  - 删除空目录：发布包/、.dist/
  - 删除 __pycache__ 目录（16 个）+ 所有 *.pyc 文件
  - 删除运行日志：dashboard_restart.log、ingest_server.log
  - 验证并删除训练数据副本（共约 14.9 GB）：
    - yolo_floodnet images/labels（8.3 GB）、yolo_rescuenet images/labels（5.4 GB）
    - yolo_landslide images/labels（1.4 GB）、yolo_floodimg images/labels（7.4 MB）
    - 删除前验证：文件数对比 + 抽样 5 个文件 MD5 一致后删除，保留 dataset.yaml
  - 删除运行时产物：stream_uploads/（推流测试图片副本）
  - 移入旧版模型备份：flood.pt（25.3 MB，未被引用）
  - 移入数据下载脚本备份：37 个下载/搜索/验证脚本（从 disaster-data 根目录移入，约 0.5 MB）
  - 移入脚本备份目录：waterlevel_diagnose.jpg（调试图）
  - 检查 yolov8n-seg.pt（7 MB）：已复制到 04-模型权重/ 目录
- **整理目录结构**（`H:\实习\U盘传输-20260831`，总大小 76.78 GB）：
  - `01-灾害链推理引擎-v2/`（31 MB）：完整项目代码
  - `02-基础设施灾损识别/`（11 MB）：完整项目代码
  - `03-训练数据集/`（66.68 GB）：10 个子集（image_datasets 8 个 + infra_datasets 2 个）
  - `04-模型权重/`（348 MB）：推理权重 + 训练产物 + EasyOCR 模型 + 旧版备份
  - `05-数据文件/`（4.38 GB）：地理数据、ERA5 回放、RLBJ、北京区划
  - `06-演示材料/`（10 MB）：综合演示全套
  - `07-文档报告/`（< 1 MB）：项目进展记录、调研报告等
  - `08-调研资料/`（5.32 GB）：开源项目/纯论文/有代码/实验
  - `09-数据下载脚本备份/`（< 1 MB）：37 个下载脚本
- **数据完整性校验**：
  - 03-训练数据集：10 个子集文件数与原目录全部一致 ✅
  - 04-模型权重：所有 .pt 文件存在，EasyOCR 2 个文件（craft_mlt_25k.pth / english_g2.pth）复制成功 ✅
  - 05-数据文件：GeoJSON 原版+简化版 ✅、ERA5 数据 ✅、demo_params.json ✅
  - 06-演示材料：16 张演示图片（实际 21 张）✅、2-推理结果.json ✅、run_demo.py ✅
  - MD5 抽样验证：water_level_digit_detector.pt / rdd2020_yolov8n.pt / zhengzhou_geojson / craft_mlt_25k.pth 全部一致 ✅
- **状态**：完成
- **备注**：本次为文件清理与复制整理，未改动任何功能代码。变更保护清单全部确认保留。无 git 提交。

### 2026-09-01｜接入山体滑坡灾害识别（fuse_infer/数据流/ingest 支持 landslide 任务）
- **改动内容**：
  - `tools/fuse_infer.py`：新增 `_infer_landslide` handler，注册到 TASK_DISPATCH（键 `landslide`/`landslide_seg`）；修复 `parse_task_arg` 使用 `rsplit` 避免 Windows 盘符冒号冲突
  - `tools/preprocess_api.py`：`map_to_bn_states` 滑坡面积映射目标从"地质易发性"（中间节点）修正为"滑坡历史密度"（根节点，三态：低/中/高）
  - `bn_engine.py`：`infer` 方法跳过证据节点（pgmpy 不允许同一变量同时出现在 query 和 evidence 中）
  - `dashboard.py`：`_bg_recognize` 新增 landslide 分支，识别后设置 `evidence_kwargs["landslide_area_m2"]`，summary 显示滑坡面积
  - `scripts/ingest_server.py`：`ALLOWED_TASK_TYPES` 增加 `"landslide"`，API 文档同步更新
  - `scripts/simulate_data_stream.py`：`IMAGE_KEYWORD_TASK` 增加 `"滑坡"/"landslide"`，`DEFAULT_IMAGE_TASKS` 增加滑坡图
  - 综合演示：补充 `滑坡-01.jpg`（CASlandslides 数据集转换），更新 `run_demo.py`（新增 landslide 任务和打印分支）、`0-演示讲解文档.md`（输入图片/映射表/材料清单）
- **验证结果**：
  - ✅ 单图验证：滑坡面积 111.84 m²，灾情等级轻度，无 error
  - ✅ 映射验证：100m²→低、300m²→中、600m²→高（`滑坡历史密度` 三态正确）
  - ✅ 回归验证：水位/道路/洪水三类识别不受影响，内涝风险结果与演示文档一致
  - ✅ BN 推理链路：滑坡历史密度 → 地质易发性 → 地质灾害概率，证据驱动正常
- **变更保护**：`_bg_recognize` 全局队列框架、双 fragment 结构、`PYTHONNOUSERSITE=1`、`from infra_path_config import`、水位公式、洪水权重、趋势预测方案、郑州地图单 trace、ERA5 磁盘缓存、DISASTER_DATA_DIR 三级回退、滑坡复用现有 ImageSegmenter——全部保留
- **状态**：完成

### 2026-09-02｜滑坡演示优化：演示图扩充/推流任务过滤与分组轮换/识别记录分类展示/阈值调整
- **改动内容**：
  - **任务A：扩充滑坡演示图** — 从 CASlandslides 数据集挑选 4 张滑坡特征明显的图片，验证通过后复制到演示目录（滑坡-02.jpg~滑坡-05.jpg），面积覆盖 72~473 m²，分别对应 BN 低/中/高三态
    - 滑坡-02.jpg（LongxiheSAT0062）：473.1 m² → 高
    - 滑坡-03.jpg（LongxiheSAT0090）：340.8 m² → 高
    - 滑坡-04.jpg（LongxiheSAT0061）：238.3 m² → 中
    - 滑坡-05.jpg（LongxiheSAT0075）：72.0 m² → 低
  - **任务B：推流脚本 --tasks 参数 + 分组轮换**（`scripts/simulate_data_stream.py`）：
    - 新增 `--tasks` CLI 参数，接受逗号分隔的任务名过滤（如 `--tasks landslide` 只推滑坡图）
    - 默认轮换策略改为分组轮换：按任务类型分组（flood/water_level/road/landslide），每轮从各组各取一张，保证每轮覆盖全部任务类型
    - 更新 docstring、--help 说明、启动日志
    - era5/random 模式行为不变，--tasks 仅作用于 image 模式
  - **任务C：dashboard 识别记录按类型分类展示**（`dashboard.py`）：
    - `_stream_image_records` 展示区改造：按 task_type 分组，water_level/road/flood/landslide 四类各占一列，每类显示最新一条（站名/类型/summary/时间）
    - 全部历史记录折叠入 expander，保留 50 条上限逻辑
    - 不改动 `_bg_recognize` 后台线程、`_BG_IMAGE_RESULTS` 全局队列、主线程合并逻辑、record 结构
  - **任务D：演示文档补充**（`0-演示讲解文档.md`）：
    - 输入图片清单增加滑坡图（共 8 张）
    - 图 4~7 山体滑坡卫星影像识别结果表（5 张图实测面积/等级/BN 证据）
    - 新增"八、山体滑坡专项演示"章节：推流命令、预期界面效果、讲解要点
    - 运行演示命令增加 landslide 示例
  - **任务E：滑坡面积阈值微调**（`tools/preprocess_api.py`）：
    - map_to_bn_states 阈值调整为 <40 不设证据、40~120 低、120~300 中、≥300 高
    - 同步更新 docstring
- **验证结果**：
  - ✅ 任务A：5 张滑坡图 fuse_infer 均输出非 None 滑坡面积（111.8/473.1/340.8/238.3/72.0 m²）
  - ✅ 任务B：`--tasks landslide` 只推 5 张滑坡图轮转；默认模式每轮 flood/landslide/road/water_level 各一张
  - ✅ 任务E：map_to_bn_states(50/150/400) 输出低/中/高，水位/道路/洪水映射不变
  - ✅ 回归：fuse_infer 洪水 1095.06 m² + 滑坡 473.13 m² 全链路正常
- **变更保护**：全部保留——`_bg_recognize` 框架/双 fragment/PYTHONNOUSERSITE=1/infra_path_config/水位公式/洪水权重/趋势预测方案/郑州地图单 trace/ERA5 磁盘缓存/DISASTER_DATA_DIR 三级回退/滑坡复用 ImageSegmenter/--tasks 仅影响 image 模式
- **状态**：完成
