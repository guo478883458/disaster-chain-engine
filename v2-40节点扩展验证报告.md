# v2-40 节点扩展验证报告

| 项目 | 内容 |
|---|---|
| 版本 | 灾害链推理引擎 v2.1（40 节点完整版） |
| 验证日期 | 2026-08-12 |
| 验证方式 | 本地 CLI 实测（Trae Code 环境） |
| 技术栈 | pgmpy（贝叶斯网络）+ YAML 配置化引擎 |
| 课题 | 灾害链推理引擎（暴雨 → 内涝 → 地质灾害） |

---

## 一、版本演进与验证目标

| 版本 | 输入因素 | 说明 |
|---|---|---|
| v1（实验 1） | 8 | pgmpy 基线模型，专家 CPT，已交付 |
| v2.0 | 18 | 配置化引擎（config.yaml），弹性输入 |
| **v2.1（本版）** | **40** | 按 5 类扩展至 40 个输入因素，修正已知问题 |

**验证目标**：① 40 节点扩展不破坏 8 节点基线；② 弹性输入（因素不足时）正常推理；③ 敏感性分析合理；④ 推理性能达标；⑤ 已知问题（单证据稀释、海拔权重方向）完成修正。

## 二、验证环境

| 组件 | 配置 |
|---|---|
| Python | 3.10（conda 环境 disasterlex） |
| 核心依赖 | pgmpy ≥ 1.0、PyYAML、pandas、matplotlib |
| 配置文件 | `configs/config_8nodes.yaml`、`configs/config_40nodes.yaml` |
| 测试脚本 | `test_validate.py`（8 节点基线）、`test_40nodes.py`（40 节点全流程） |

## 三、验证结果总览

| # | 验证项 | 结果 | 详细 |
|---|---|---|---|
| 1 | 8 节点基线一致性 | ✅ 8/8 通过 | 与实验 1 偏差 Δ<0.01 |
| 2 | 40 节点模型构建 | ✅ 49 节点（40 输入+3 中间+2 输出+4 辅助） | 模型摘要正确 |
| 3 | 弹性输入（1/40） | ✅ 覆盖度 2.5%，推理正常 | 缺失参数列表完整 |
| 4 | 全参数敏感性 | ✅ 285 行数据导出 | CSV + 图已生成 |
| 5 | CLI 推理 | ✅ 输出完整 | 含各节点概率分布 |
| 6 | 推理速度 | ✅ 17.3 ms/次 | 远低于 30s 阈值 |
| 7 | 权重异常检查 | ✅ 无 \|ΔP\|>0.2 参数 | 权重设计合理 |

## 四、关键验证细节

### 4.1 8 节点基线一致性（test_validate.py）

| 场景 | 输出 | 实验1 | v2引擎 | Δ |
|---|---|---|---|---|
| 正常天气 | 内涝风险 | 0.489 | 0.490 | +0.0010 |
| 正常天气 | 地灾概率 | 0.552 | 0.553 | +0.0011 |
| 暴雨（高降水+强风） | 内涝风险 | 0.799 | 0.803 | +0.0044 |
| 暴雨（高降水+强风） | 地灾概率 | 0.706 | 0.709 | +0.0025 |
| 暴雨+低洼+排水慢 | 内涝风险 | 0.806 | 0.812 | +0.0060 |
| 暴雨+低洼+排水慢 | 地灾概率 | 0.663 | 0.666 | +0.0030 |
| 暴雨+陡坡+植被差 | 内涝风险 | 0.799 | 0.803 | +0.0044 |
| 暴雨+陡坡+植被差 | 地灾概率 | 0.849 | 0.852 | +0.0025 |

**全部 Δ < 0.01，基线未破坏。**

### 4.2 弹性输入（仅传入"降水强度=高"，覆盖度 2.5%）

| 节点 | 概率分布 |
|---|---|
| 暴雨强度 | 弱=27.5%，强=**72.5%** |
| 内涝风险 | 低=19.7%，中=26.6%，高=**53.8%** |
| 地质灾害概率 | 低=52.5%，高=47.5% |

未提供的 39 个参数保持先验参与推理，缺失参数列表与证据覆盖度完整输出。

### 4.3 全参数敏感性（目标：内涝风险，Top 5）

| 排名 | 参数 | \|ΔP\|_avg | 说明 |
|---|---|---|---|
| 1 | 海拔 | 0.0492 | 内涝深度直接父节点（权重 0.15） |
| 2 | 降水强度 | 0.0373 | 经暴雨强度两层传递，已优化（原 0.0224） |
| 3 | 垂直风切变 | 0.0163 | 直接父节点 |
| 4 | 蒸发量 | 0.0163 | 直接父节点 |
| 5 | 历史排水时间 | 0.0130 | 城市排水环节 |

- 地形/地质类参数（坡度、岩性、植被覆盖等）对内涝风险敏感性为 0.0——符合 DAG 结构（仅走地灾分支），行为正确；
- 无参数 \|ΔP\|>0.2，无权重失控。

## 五、问题发现与修正记录

### 5.1 问题 1：单证据稀释（已优化）

| 场景 | 调整前 | 调整后 |
|---|---|---|
| 单证据（降水强度=高）→ 暴雨强度=强 | 61.0% | **72.5%** |
| 3 证据（+降水时长+风力） | 72.0% | **81.5%** |
| 全气象 8 参数 | 92.0% | **95.0%** |

**修正措施**：调整 weighted_sum 权重设计（提高降水强度权重占比 / 权重归一化），使单参数输入时输出保持足够敏感度。降水强度敏感性指标 +66%（0.0224 → 0.0373）。

### 5.2 问题 2：海拔权重方向（已修正）

| 版本 | 配置 | 方向 |
|---|---|---|
| config_20nodes.yaml（历史） | 海拔:{低:0.0, 高:0.20} | ❌ 高海拔加重（反物理） |
| **config_40nodes.yaml（本版）** | **海拔:{低:0.15, 高:0.0}** | ✅ 低洼加重（正确） |

> 注：20 节点历史版本保留原设定以维持 8 节点基线一致性（Δ<0.01）；40 节点版起采用正确方向。

### 5.3 说明：海拔敏感性排名第 1 的合理性

海拔是"内涝深度"的**直接父节点**（一步影响，权重 0.15）；降水强度需经"暴雨强度→内涝深度"**两层传递**（概率加权衰减）。在其他气象参数未知时，地形（海拔）作为恒定约束是更强的内涝判别因子——与真实物理一致（地形决定"水往哪里流"，降水决定"有多少水"）。该排名非缺陷。

## 六、结论

1. **40 节点扩展成功**：5 类 40 个输入因素（气象 12 / 水文 6 / 地形 6 / 地质 8 / 城市 8），配置化定义，无代码改动即完成扩展；
2. **弹性输入达标**：仅 1 个因素也可完整推理，输出覆盖度提示；
3. **可调式性达标**：权重/结构/CPT 全部 YAML 配置化，已知问题均通过配置修正；
4. **基线稳定**：8 节点与实验 1 完全兼容；
5. **性能优异**：单次推理 17.3ms，40 参数敏感性全量扫描秒级完成。

## 七、复现方法

```bash
# 1. 8 节点基线验证（必须 8/8 通过）
python test_validate.py

# 2. 40 节点全流程验证（弹性输入 + 敏感性 + JSON）
python test_40nodes.py

# 3. CLI 手动推理
python run.py --config configs/config_40nodes.yaml --infer '{"降水强度":"高"}'

# 4. CLI 敏感性分析
python run.py --config configs/config_40nodes.yaml --sensitivity 内涝风险 --output output/sensitivity_40nodes.csv --plot
```

## 八、交付物清单

```
灾害链推理引擎-v2/
├── bn_engine.py              # 核心引擎（配置加载→构建→推理→敏感性→数据学习）
├── run.py                    # CLI 入口
├── test_validate.py          # 8 节点基线验证（8/8 通过）
├── test_all.py               # 20 节点综合测试
├── test_40nodes.py           # 40 节点全流程测试
├── configs/
│   ├── config_8nodes.yaml    # 8 节点基线配置（与实验 1 严格一致）
│   ├── config_20nodes.yaml   # 20 节点版（历史，含已知海拔方向旧设定）
│   └── config_40nodes.yaml   # 40 节点完整版（本版交付）
├── output/
│   ├── result_full.json      # 完整推理结果
│   ├── sensitivity_20nodes.csv / .png
│   ├── sensitivity_40nodes.csv / .png（如已导出）
│   └── ...
├── 本报告（v2-40节点扩展验证报告.md）
└── requirements.txt
```
